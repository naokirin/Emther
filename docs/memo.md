# MEMO

## TODO

* [対応済み] Journalで、テキストから検出されたメンバー名を確実に「人物」にすべて登録する → 保存前の候補検出（`ensureNameCandidatesAllowed`）は生テキストの形態素検出しか見ておらず、ローカルモデル抽出（`structured.people`）だけが拾った未登録名は`getPersonId`で解決できずに静かに`people`から落ち、登録ヒントにも出ないまま取りこぼされていた。抽出結果側の未登録名も一度きりの登録ヒント（`JournalNameCandidateSuggestion`）候補に合流させ、これまでヒントが単発投稿（POST /api/journal）にしか無かった「まとめて記録する」「Observation Dumpの採用」でも同じヒントが出るようにした（自動登録はしない既存方針は変更なし）。
* [対応済み] Journalの分析の場合に、Issueになるかそうでないかという判定が強く、Issue化する/しない場合どちらでもEMに寄り添う回答結果を増やすようにしたい → `buildJournalAnalysisTask`（EM明示の手動分析タスク文）を改修し、Issue化判定は一覧に残すかどうかの分類に過ぎないと明記したうえで、issue/watch/dismissいずれの判定でもEMがこの状況にどう向き合うとよいかという実務的な気づき・助言を回答本文に必ず書くよう指示。あわせてこのタスク文にだけ抜けていた`watch`（監視継続）判定も明示し、issue/dismissの二値だけで終わらせないようにした。
* [対応済み] Journalに、テキスト入力時点で「感想を含む」をチェックボックスでチェックできるようにして、事実以外の単なる感想みたいなものを入力しやすくしたい。記録時には選択されていると「[感想を含む]」がテキストの先頭につくようにする。 → 単発投稿の3画面（Dashboardの「メモする」、/journalのクイック入力、人物詳細のJournal追記）にチェックボックスを追加し、チェック時は送信テキストの先頭に「[感想を含む] 」を付けて保存するようにした。
* [対応済み] 提案やJournalのリンクに関して、カスタムUIのツールチップでタイトルだけ表示したい → 文中の`#ID`参照リンク（`IdFragmentLink`/`IdLinkedText`。相談やJournal本文中でのID言及を解決するリンク）は、これまで表示テキストがID断片のみでリンク先の中身（提案・Journalのタイトル）が分からなかった。ネイティブ`title`ではなく既存の`.axisTooltip`（`data-tooltip`属性を読むCSSカスタムツールチップ、`IssueStatus.tsx`/`PersonScoreBadge.tsx`と同じ仕組み）に揃え、ホバー/フォーカス時にだけ`/api/id-resolve`へ問い合わせてタイトルのみを表示するようにした（複数候補時はタイトルを改行区切りで並べる。取得前は`.axisTooltip`クラス自体を付けず、空の吹き出しが一瞬見えるのを防止）。
* [対応済み] 提案の一覧でキーワード検索できるようにしてほしい → `/suggestions`一覧は確認状態・確認優先度のチェックボックスフィルタのみでキーワード検索が無かった。件数は`useSuggestions`で全件取得済み（Journalのようなサーバー側ページネーションではない）のため、Journal一覧の検索UI（`styles.field`の入力欄）に揃えつつ、フィルタ自体はクライアント側で行う`suggestionMatchesKeyword`（`lib/types.ts`）を新設。タイトル・メモ（`memos[].text`）・詳細（`detail.conclusion`/`facts`/`logic`/`advice`）を対象に大小文字を区別しない部分一致検索とし、確認状態・確認優先度フィルタの一番上に検索欄を配置した。