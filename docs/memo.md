# MEMO

## TODO

### UI
* [対応済み] 学びの提案の今すぐ生成するボタンがAIで生成中でも非アクティブになったりしない → ボタン自体はdisabled化されておりクリックはブロックされていたが、`.btnOutline`に`:disabled`の見た目（不透明度・カーソル）が無く「変わっていないように見える」だけだった。`.primaryBtn`と同じdisabledスタイルを追加した。
* [対応済み] 学びの提案の結果のテキストが読ませる向けなのにフォントサイズが小さすぎる → 読ませる本文（rationale/根拠/参考文献）のフォントサイズを引き上げた。
* [対応済み] 今日タブでAIに戦略を提案させるときに提案中でタブを切り替えたりすると、提案結果が残らない → 生成中フラグ・結果はTodayActionsPanel（ダッシュボード／書き連ねタブ切替でアンマウントされる）のローカルstateだったため。親のDashboardPageInner（タブ切替で不変）側のstateへ引き上げ、propsで受け渡すようにした。
* [対応済み] タブ移動してしまうと相談の入力中テキストが消えたりしてしまうので、残るようにしたい → 「何でも相談」フォームの下書きをこの端末のlocalStorageにも退避し、画面遷移で再訪した際に復元するようにした。
* [対応済み] 今日タブのところから、要注目Journalの項目のリンク先に飛ぶと、Journalのテキスト入力状態の相談画面に飛ばされる事がある → 「要注目Journal」カードはまだ相談が存在しないJournalが対象のため、相談画面（テキスト入力状態）ではなくJournal自体（focus指定）へ遷移するよう変更した。
* [対応済み] 現場メモというタブとJournalがユーザーにとって結びつかない可能性があるため、「ジャーナル」に表示する用語統合したい → タブ名・ページタイトル・ヘルプ文言の「現場メモ」表記をすべて「ジャーナル」に統一した。
* [対応済み] 以前からあるが、Journalを書き込んだ直後になぜか書き込んだばかりのJournalが一覧で2番目以降に表示されることが多々ある → 「今日」の日付（まとめ入力の発生日マーカー省略時・単発投稿の発生日入力で今日を選んだ場合）が正午のタイムスタンプに丸められており、正午以降に書いたエントリが同日の他エントリより前に記録された扱いになっていたのが原因。「今日」は日またぎの曖昧さが無いため正午丸めをやめ、実時刻をそのまま使うようにした。
* [対応済み] 提案一覧のフィルタを複数選択式にしたい → 確認状態・確認優先度の単一選択フィルタ（Select）をチェックボックス群に置き換え、複数選択できるようにした。
* [対応済み] 「提案」の詳細にある「判断・提案（Agent）」や「壁打ち」にも元の相談の内容を反映し、やり取りを継続でできるようにしたい → 提案専用のAgent Run（linkedRun）が無い場合、従来は「元の相談を開く」への片道リンクのみだった。linkedRunが無ければ元の相談（sourceConsult）をそのまま表示・継続の対象にするようにした。

### ローカルAI
* [対応済み] Journalで妙にネガティブが選択されやすい一方で、ポジティブの選択がされにくい感覚がある → 感情判定のfew-shot例にneutralの例が1件も無かったため。neutral例を追加し、「明確にネガティブと言い切れる場合だけnegativeにし、迷ったらneutralにする」という指示をプロンプトに明記した。

### 実名隔離機能
* [対応済み] 実名を含んでしまっていた場合に自動で隔離されたJournalをユーザーが確認できるようにしたい → 自動隔離（実名リーク検知）と手動アーカイブが`archived_at`だけでは区別できなかったため、`archived_reason`列を追加し自動隔離時だけ`"name_leak"`を設定。Journal一覧に「🔒 実名隔離のみ表示する」フィルタを追加した。

### 不具合
* [対応済み] 相談履歴で相談を切り替えたときに「Journalの紐づけがない」ものに切り替えると紐づかない誤ったJournalが表示される → 選択中の相談が切り替わってもuseJournalEntryの前回取得結果（sourceJournal）が残り続け、OriginTraceがそれを無条件に信頼していたのが原因。表示中のsourceJournalが現在選択中のrunのsourceJournalIdと一致する場合だけ使うようにした。
