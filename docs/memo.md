# MEMO

## TODO

### バグ

### UI / UX 改善

### 機能追加
* メンバーの詳細でも長期プロファイルを入力できるようにしたいです
* Journal等から見つかった情報をもとに、コンテキスト化したものなどをまとめてみたいが見れる場所がない。
* 確認したが「確認しなくてよかった」を人間側から示せない・UI上の強調を減らせないものがある（メンバーのアラート表示、評価ログ、Journalのネガポジなど）。確認したが対応は不要だったなどを入力・UIに反映されるようにする仕組みを入れたいです

### 情報設計・運用改善
* EMとしてのIssueと、備忘に近いタスク系のものが混在すると大量にやることが積まれてしまい、管理がむずかしい。このあたりはどうにかしたいかも
* [対応済み] Journal（日常の観測）、Issue（発見した課題・その管理）、方針・目標（中長期の戦略）、ふりかえり（気づきの記録）という情報分割は良いですが、EMが必要に応じてそれぞれを行き来し続けることになり、認知負荷が高い。情報の登録・管理単位と、実務における横断的な情報の取得の運用をわけて設計する必要がありそうです。レポートなどはその1つですが、リアルタイムな情報ではないのでリアルタイムなものがあると良さそうです。 → 2026-09-14に方針確認済み: 論点は「今日何を見るべきか」ではなく「戦略→Issue→Journalの縦の接続を追いかけづらい」ことだったため、Dashboardの再構成ではなく、(a) Issue詳細・Journal（/journal）にObjective›KeyResult›Issue›Journalの双方向パンくず（StrategyTrail、Issue.keyResultId/sourceJournalId・JournalEntry.resolvedIssueIdというライブな外部キーのみから組み立て、OrgTheme.evidence*の一度きりAIスナップショットには依存しない）、(b) 「方針・目標」タブに閲覧専用サブタブ「つながりを見る」（/org/thread）を追加し、Objective→KeyResult→Issue→Journalをライブクエリのツリーとして一望できるようにした。/org（管理面）自体は変更していない（肥大化させない方針）
