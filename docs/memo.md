# MEMO

## TODO

### AIサポート

* [対応済み] Journal入力時に自動で関係者名も設定してほしい → 既登録の人物名は従来どおり自動で紐づく。未登録の人物名らしい語句は保存をブロックせず、投稿直後だけ1クリックで登録・紐付けできるヒントを出すようにした（QuickJournalNoteForm／JournalDumpPanel／PersonJournalComposer）。
* [対応済み] Journalからすでに提案が生成されていても、提案をさらに作成する案内が出たりすることがあるので、紐づく提案がすでにあるJournalは確認案内をなくす/弱めることをしたい → sourceConsultRunIdが付いたJournal（＝すでにLead Agent runが生まれている）は、ダッシュボードの「要注目Journal」カード・「気になること」欄には出さないようにした。
* [対応済み] 相談で、別口で提案を残している場合に、追いかけないようにするのに「却下」するしかない。却下だけでなく、「対応済み」みたいなものも欲しい → 「他の提案への追記提案」に「対応済みにする」を追加（却下とは別に、runのログへ記録を残す）。
* [対応済み] 相談、Journal、提案を削除（アーカイブ）したい。重複してしまった場合や誤って追加した場合などに非表示にしたり、AIに考慮させないようにできるため → 3種とも既存の状態（reviewStatus/triageStatus等）とは独立の「アーカイブ」フラグを追加。一覧からは既定で除外（表示トグルあり）、Vitals/Dashboard/朝サマリー等のAIの判断材料からも除外する。記録・履歴自体は削除しない。

* [対応済み] JournalのAIでの分析結果として、メンバーの長期プロファイルに入れるほうが良いものがあれば、入れるようにする → 気軽に書けることを優先し、クラウドのLead Agent分析へはエスカレートせず、既存のタグ/人物/緊急度抽出と同じローカル1回の呼び出しに相乗りさせた。「一時的な出来事」ではなく「長期的な傾向」と判定された場合だけ、投稿直後に💡ヒントとして出し、1クリックで人物の長期プロファイル（既存のPOST /api/knowledge/interpretations）へ採用できるようにした（対象は既登録の人物のみ。未登録人物の自動登録はしない既存方針を踏襲）。

### UI

* [対応済み] 細かいところでIssueという名称が残っているのですべて修正する → UI表示文言（画面文言・エラーメッセージ・ラベル）のIssueをすべて提案に置換。変数名・型名・ルート（/issues等）・コード内コメント・AIへのプロンプト文言は対象外。
* [対応済み] 各画面で提案のリンクを踏んだときのデフォルト挙動をサイドピークにする → app/layout.tsxに提案サイドピークをグローバルに設置（SuggestionPeekRoot）。IdFragmentLink・JournalEntryCard・StrategyTrail・StrategyThreadTree・PersonRecordsSection・TeamEditPanel・useGoToRunIssue等、提案詳細への主要な導線を全画面共通のサイドピークに統一した。
* [対応済み] 「他Issueへの追記提案」で追記対象を個別に選択できるようにする → チェックボックスで個別に選んで「採用して追記する／対応済みにする／却下する」を選択できるようにした。
* [対応済み] Journalがやはり書き込み順にならないので、書き込み直後に見失ってしまう。修正したい。 → 新しいJournalは並び順としては常に先頭に来るが、/journalで他のページ（2ページ目以降）を見ている最中に記録すると今見ているページのままで見失っていた。記録直後は1ページ目へ戻すようにした。
* [対応済み] 相談で「提案として残す」と履歴から消える → 提案化後も相談履歴に残すよう変更。相談からは sourceRunId のみで提案を作り（agentRunId に吸収しない）、分割起票や提案に紐づかない続きの壁打ちが同じスレッドでできるようにした。
* [対応済み] 今日の状況に表示するメンバーを自分の管理するチームのメンバーだけにする → チーム・メンバーの状態チップ／過去との比較の対象人物を、自分が管理するチーム（Team.managedByEm、兼務含む）のメンバー（PersonSummary.isDirectReport）だけに限定した。
* [対応済み] EMの成長のチェックインの推移で、同じ日に複数の項目でおなじ数値を選択すると重なって見えなくなるので、点を少しずらして表示したい → Chart.jsのプラグインで、実データ・目盛りは変えず描画位置だけを項目（気分/エネルギー/ストレス）ごとにピクセル単位でわずかに左右へずらすようにした。

* Journalで個人名が混じってしまった場合に、相談でAIチャットが使えなくなる。そこまでは良いが、個人名を除いてJournalを編集し直しても同じ相談に接続されてAIを再度実行させることができない。相談をリセットして最初からやり直せると良さそうなのでその機能が欲しい