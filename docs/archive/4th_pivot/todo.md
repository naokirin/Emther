### 機能追加
* [x] [優先度中]入力した情報（議事録など）をローカルLLMでサマリーにできる → 手直ししたうえで登録できる機能を追加する（Journalと同じにするか別にするかは要検討）
  - 対応完了: `local-summarizer.ts` / `/api/journal/local-summarize` / `LocalLogSummaryImporter.tsx` を実装。機微情報を外部・ストレージに溜めず、ローカルで抽出・要約したシグナルをEMが手直ししてJournalに登録可能。

* [x] [優先度中]社内用語を登録しておき、AIが参照できるようにしたい
  - 対応完了: `glossary-store.ts` / `/api/glossary` / `GlossaryPanel.tsx` を実装し、組織画面（`/org`）から管理可能に。Agent RuntimeのシステムプロンプトおよびローカルLLM要約プロンプトに社内用語コンテキストを自動注入。

* [x] [優先度低]どこが更新されたかわかりにくいので、最近更新されたものに「New」とかがつくようにしたい
  - 対応完了: `suggestions/page.tsx`（提案一覧）、`ConsultHistoryItem.tsx`（相談履歴）、`TodayActionsPanel.tsx`（今日やるべきこと）に24時間以内更新判定の「NEW」/「新着」バッジを付与。

* [x] [優先度低]エージェントが動いてる、ということが見えたほうがユーザーにとっても裏の動きがわかって良さそうなので、今日タブとかで相談タブのエージェント以下に埋もれているエージェントの状態・直近の動きを見せるようにする
  - 対応完了: `AgentStatusSection.tsx` を実装し、今日タブ（`TodayActionsPanel`）に配置。巨大な思考プロセスは出さず、エージェント稼働状態（待機中/実行中）と直近の自律的な動き（最新3件）、最終巡回時刻をコンパクトに可視化。

### 機能修正
* [x] [優先度中]「この相談への結論」などで選択して提案として残すや追記するをしたあとに、選択して提案として追加したものがそのまま再度提案可能なものとして残り続けるので、一度提案に追加したものは、提案済みとして再追加できないようにしたい
  - 対応完了: `ConsultReviewPanel.tsx` にて、選択されたRunから作成済みのIssue/提案タイトルを照合し、「提案済み」バッジの表示、チェックボックスの非活性化、全件追加済み時のボタン非活性化制御を実装。

### UI改善
* [x] [優先度中]「結論」は重要な情報であるが、文字サイズが小さすぎるので、サイズを見直したい。ただ、文章量も多いので、単にサイズを大きくすると、大量の情報になってしまうため、UIとしての見せ方やUXとしてどういうふうに見せるか検討して、単なる文字サイズ変更ではなく、「結論」セクションの見せ方の改善を進めたい
  - 対応完了: `ProposalBlock.tsx` および `SuggestionDetailContent.tsx` において、結論（Hero Conclusion）を目立たせ、ファクト・ロジック・代替案・Expand/Challengeなどの思考プロセスは `<details>`（折りたたみ・段階的開示）としてデフォルト非表示化。

* [x] [優先度低]「提案の詳細」ので「判断・提案」セクションと「壁打ち」セクションが二段組になるので、縦長になり、文章全体を読みにくい。一方で、並んで見える事によりそれぞれを確認しながらの操作がしやすいメリットや、狙ってではないが、段組が変わることで区切りのわかりやすさもあるため、改善方針を検討して、より読みやすく操作しやすいUI/UXにできればその対応をしたい
  - 対応完了: `SuggestionDetailContent.tsx` にビュー切り替えタブ（`[⚖️ 並べて表示] [📋 判断・提案] [💬 壁打ち]`）を導入。二段組での並行確認と、単一カラムでのゆったりとした全幅閲覧をワンクリックで切り替え可能に。

### 方針変更
* [x] [優先度中]現状でテーマがあまり機能していない。AIに出させるよりEMに「EMとして今の自分のテーマはこれだ」というものを書いてもらって、それと紐づけるほうが良いのかも。可能ならそのテーマを壁打ちしながら作成できるとよりGood
  - 対応完了: `theme-store.ts` / `/api/themes` POST / `OrgThemesPanel.tsx` を実装し、EM自身が向き合いたい注力テーマを直接登録・採用可能に。さらに「💬 AIと壁打ち」ボタンから `/chat?prefill=...` へワンクリックで遷移し、テーマの言語化・精緻化を対話で進められるフローを整備。
  - 加えて、Journalの再定義（日中の随時メモ維持 ＋ 1日の終わりのAI問いかけによる振り返り `DailyReflectionForm.tsx`）も完了。

### 不具合
* [x] [優先度低]相談画面のタブのツールチップの表示判定の範囲がおかしい
  - 対応完了: `TopNav.tsx` のサブタブコンテナ `<nav className={styles.subTabs}>` に誤って付与されていた `.axisTooltip` および `data-tooltip` を除去し、サブタブ全体に意図しない巨大なツールチップが覆いかぶさる不具合を解消。

### のちのち
* [後で検討]Codex / OpenCode などの有名な他のサブスク系AIもできればサポートに入れたい