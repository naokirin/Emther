// フェーズ3.1: 新SPA（Vite + React Router）の骨組み。
// 画面移植は3.4（移行順位付け）・3.5（画面単位移植）で行う。
export function App() {
  return (
    <main>
      <h1>Emther</h1>
      <p>Vite SPA skeleton（docs/2nd_architecture/plan.md フェーズ3.1）</p>
    </main>
  );
}
