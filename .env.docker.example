# Docker 実行時の任意設定例。
# コピーして使う: cp .env.docker.example .env
# （.env は gitignore 済みの想定。リポジトリ直下に置く）

# ホスト側で公開するポート
EM_PORT=3000

# --- 任意: 対話ログインの代わりに使う認証（どれかがあれば Agent Run の主経路が動く）---
# ANTHROPIC_API_KEY=
# CLAUDE_CODE_OAUTH_TOKEN=
# CURSOR_API_KEY=
